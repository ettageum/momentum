// #3.0 The Document Object

// type [document], [console.dir(document)] and [doucment.title] in console section.

// type [document.title = "Hi"] then check browser title.

// HTML과 js는 browser상에서 유기적으로 연동되어 있다. 따라서 서로 간에 연결을 위해 따로 해 줄 노력은 없다.
